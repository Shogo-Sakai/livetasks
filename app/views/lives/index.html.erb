<% cache @current_user, expires_in: 1.minutes, skip_digest: true do %>
  <%= render "lives/header" %>
<% end %>
  <div class="row contents">
    <% cache @current_user, expires_in: 1.minutes, skip_digest: true do %>
      <%= render 'lives/sidenav', class:"col " %>
    <% end %>  <div class="main-contents-new ">
      <div class="main-contents-new__input">
        <div class="main-contents-new__input__box notice-bord">
          <%= render "lives/notice_contents"%>          
        </div>
      </div>
    </div>
  </div> 
</div>